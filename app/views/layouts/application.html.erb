<!DOCTYPE html>
<html>
<head>
  <title><%= content_for?(:title) ? yield(:title) : "PinewoodDerby" %></title>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="chrome=1">
  <%= stylesheet_link_tag    "application" %>
  <%= javascript_include_tag "vendor/modernizr" %>
  <%= csrf_meta_tags %>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="icon" type="image/png" href="/favicon.png">
  <!-- icon credit: http://www.areskub.com/ sieze icon set -->
</head>

<body>
  <header id="header" class="top-bar" data-topbar>
    <ul class="title-area">
      <li class="name"><h1><%= link_to 'Pinewood Derby', root_path %></h1></li>
    </ul>
    <nav class="top-bar-section">
      <ul class="right">
        <li class="<%= 'active' if current_page?(root_path) %>"><%= link_to 'Welcome', root_path %></li>
        <li class="<%= 'active' if current_page?(board_path) %>"><%= link_to 'Dashboard', board_path%></li>
        <li class="<%= 'active' if current_page?(contestants_path) %>"><%= link_to 'Contestants', contestants_path%></li>
        <!-- <li class="<%#= 'active' if current_page?(races_path) %>"><%#= link_to 'Races', races_path%></li> -->
      </ul>
    </nav>
  </header>
  
  <div class="container">

    <%= render 'layouts/messages' %>

    <%= yield %>

    <hr>
    <footer>
      <p class="chrome-frame-required alert alert-warning" style="display: none">
        PinewoodDerby requires <a href="http://www.google.com/chromeframe" target="_blank">Google Chrome Frame</a> for Internet Explorer.
        <br>
        Please install Chrome Frame to escape the Twilight Zone.
      </p>
    </footer>
  </div> <!-- /container -->
  <%= javascript_include_tag "application" %>
</body>
<%= javascript_tag do %>
  window.alerts = new Announcer({type: '<%= current_page?(board_path) ? "dashboard" : "alerts" %>', stats: <%= Dashboard.to_json.html_safe %>})
<% end %>
</html>
